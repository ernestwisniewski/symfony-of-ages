<nav class="navbar">
    <div class="nav-container">
        <div class="nav-logo">
            <a href="{{ path('app_homepage') }}">
                <h2>SOA</h2>
            </a>
        </div>
        <div class="nav-menu">
            <a href="#features" class="nav-link">Tech Tree</a>
            <a href="#about" class="nav-link">Civilizations</a>
            <a href="#contact" class="nav-link">Multiplayer</a>
            {% if app.user %}
                <a href="{{ path('app_games') }}" class="nav-cta">My Games</a>
            {% else %}
                <a href="{{ path('app_login') }}" class="nav-cta">Play Now</a>
            {% endif %}
        </div>
        <button class="nav-toggle">
            {{ ux_icon('lucide:menu') }}
        </button>
    </div>
</nav> 